import './globals.css'

export const metadata = {
  title: 'Flores Marina',
  description: 'Arranjos florais únicos, feitos à mão',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
